# Streamlit theme — Apple HIG (cost-first dashboard).
[theme]
primaryColor = "#007AFF"             # Apple system blue (links, buttons)
backgroundColor = "#F2F2F7"          # Apple grouped background
secondaryBackgroundColor = "#FFFFFF" # cards / sidebar
textColor = "#1C1C1E"
font = "sans serif"
